# Kreiranjes skripte/migracije

dotnet ef migrations add SeedInitialData --project .\ManiFest.Services --startup-project .\ManiFest.WebAPI

# Pokretanje migracije

update-database

# Generisanje .g dijela klase

dart run build_runner build